$(document).on('ready turbolinks:load', function(){
    var pathArray = window.location.pathname.split('/');
    var id_page = pathArray[2];
    var token = $( 'meta[name="csrf-token"]' ).attr( 'content' );
        $.ajaxSetup( {
            beforeSend: function ( xhr ) {
                xhr.setRequestHeader( 'X-CSRF-Token', token );
                }
        });
    var url_ev_1 ="/lots/"
    var url_ev_2 ="/events"
    var url_ev_3 =".json"
    var url_ev_3_b ="/new"
    $('#calendar').fullCalendar({
        events: url_ev_1 + id_page + url_ev_2 + url_ev_3,
        eventLimit: true,
        firstDay: 1,
        views: {
                dayGrid: {
                    eventLimit: 1
                }
            },
        eventRender: function(event, element) {
                element.css("font-size", "1.2em");
                element.css("padding", "1.5em");
            },
        header: {
            center: 'addEventButton'
            },
          customButtons: {
            addEventButton: {
              text: 'Reservar Fecha',
              click: function() {
                $.ajax({
                  url: '/lots/'+ id_page + '/events/new',
                  type: 'GET',
                  dataType: 'script'
                })
              }
            }
          },
        eventDrop: function(event, delta, revertFunc) {
            if (event.start.format() < moment().format()){
                alert('no no no')
                revertFunc()
            }
            else {
                $.ajax({
                    url: '/lots/:lot_id/events/'+ event.id,
                    type: 'patch',
                    dataType: 'script',
                    data: {
                        event: {
                            start: event.date_parking_start.format() ,
                            end: event.date_parking_end.format() 
                        }
                    }
                })
            }
        },

        eventClick: function(event, jsEvent, view) {
                jsEvent.preventDefault()
                $.ajax({
                    url: '/lots/:lot_id/events/'+ event.id +'/edit',
                    type: 'GET',
                    dataType: 'script'
                })
            
        }

            
    })

})





